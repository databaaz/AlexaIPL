- args:
    sql: CREATE TABLE "matches"("id" integer NOT NULL, "season" integer NOT NULL,
      "city" text NOT NULL, "date" date NOT NULL, "team1" text NOT NULL, "team2" text
      NOT NULL, "toss_winner" text NOT NULL, "toss_decision" text NOT NULL, "result"
      text NOT NULL, "dl_applied" numeric NOT NULL, "winner" text NOT NULL, "win_by_runs"
      integer NOT NULL, "win_by_wickets" integer NOT NULL, "player_of_match" text
      NOT NULL, "venue" text NOT NULL, "umpire1" text NOT NULL, "umpire2" text NOT
      NULL, "umpire3" text NOT NULL, PRIMARY KEY ("id") )
  type: run_sql
- args:
    name: matches
  type: add_existing_table_or_view
